# Commits that changed formatting and nothing else. `git blame` skips them, so
# a line still shows whoever actually wrote it rather than whoever ran the
# formatter.
#
# GitHub reads this file automatically. Locally it needs telling once:
#
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Only add a commit here if it is provably formatting-only — for the one below,
# every reformatted file was checked to equal `prettier(previous version)`.

# chore: adopt Prettier, and make CI enforce it (#235)
4878d6fabe1a683bd160c19dfafe4d66a35ce1ff
