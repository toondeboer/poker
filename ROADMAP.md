# ROADMAP

Tracks proposed and completed improvements for the Poker Blinds Buzzer monorepo. Most items
originate from the architecture/due-diligence review of the codebase.

**Maintenance convention (read this):** Claude (and contributors) must **read this file at the
start of a work session** and **keep the Status column current** — when an item is implemented,
partially landed, deferred, or abandoned, update its row in the *same* change. Add newly
proposed improvements as new rows. See [CLAUDE.md](./CLAUDE.md).

**Status legend:** ✅ Done · 🚧 In progress · ⬜ Not started · ⏸️ Deferred

_Last reviewed: 2026-06-15._

## Completed (branch `refactor/post-review-cleanups`)

| ID | Change | Impact | Risk | Description | Status |
|----|--------|--------|------|-------------|--------|
| R1 | Remove duplicate mobile lockfile | Med | Low | Deleted the tracked `apps/mobile/package-lock.json`; npm workspaces use the single root lockfile. Added a `.gitignore` guard for nested lockfiles. | ✅ Done |
| R2 | Remove dead code | Low | Low | Dropped `defaultBlindLevels` (core, 0 consumers) and the unused `BlowAnimationView` (mobile `TimerExpirationAlert`). | ✅ Done |
| R3 | De-duplicate `formatTime` (mobile) | Low | Low | `PokerTimer` imports core `formatTime` instead of a byte-identical local copy. | ✅ Done |
| R4 | Correct docs | Med | Low | ARCHITECTURE haptics row → RN `Vibration` (not `expo-haptics`); mobile README → RN `StyleSheet` (not Tailwind). | ✅ Done |
| R5 | `@poker/core` test suite | High | Low | Vitest + 28 unit tests (blinds, time math, storage factories). New turbo `test` task + root `test` script. | ✅ Done |
| R6 | Lint `@poker/core` | Med | Low | ESLint flat config + `lint` script; closes the unlinted blind spot. | ✅ Done |
| R7 | Fix web RSC boundary | Low | Low | Moved `"use client"` onto `LandingPage`; `/` is now a server component rendering it. | ✅ Done |
| R8 | Stabilize timer interval | Med | Low | `callbacksRef` in `useTimerEngine` stops per-render teardown/rebuild of the 1s interval. | ✅ Done (wants device smoke test) |
| R9 | Throttle AsyncStorage writes | Med | Med | Stopped per-second `timeLeft` writes; persist on meaningful transitions only, derive `timeLeft` from `endTime` on load. | ✅ Done (wants device verification) |

## Proposed (not started) — roughly by priority

| ID | Change | Impact | Risk | Description | Status |
|----|--------|--------|------|-------------|--------|
| R10 | Resolve privacy-policy vs Google Analytics contradiction | High | Med | Web loaded GA (`G-13MH57QZWG`) on every page while the policy claimed "no analytics"; EU publisher, no consent banner. Added `AnalyticsConsent` (cookie-consent banner that gates GA loading on opt-in, persisted to `localStorage`) and rewrote the privacy-policy sections to accurately disclose the website's optional, consent-based Google Analytics use vs. the app's true no-analytics behavior. | ✅ Done |
| R11 | Add CI (typecheck + lint + test) | High | Low | Added `.github/workflows/ci.yml`: GitHub Actions running `npm run typecheck/lint/test` (turbo tasks across all workspaces) on PRs and pushes to `main`. | ✅ Done |
| R12 | ~~Fix iOS background-expiry blind advancement~~ (invalid premise) | High | High | **Won't do.** Premise was wrong: the intended product behavior is to advance **exactly one** blind level per expiry and then **stop and wait for explicit player acknowledgement** — blinds must never silently fast-forward through multiple levels or run to the end. That single-advance-then-acknowledge behavior already exists. (iOS can't sound an alarm in the background, so the repeating notifications are the deliberate "blinds went up" nudge until the app is reopened — see R16.) Investigating this surfaced two real, unrelated bugs, fixed here: (1) `resetTimer` persisted a stale `timeLeft` from its closure, and (2) the foreground reloaded timer state from storage every second (`loadTimerState` is an unstable effect dep) — together these made "reset while paused" appear to do nothing. Fixed by persisting explicit reset state and gating the reload behind a real background→foreground transition (`wasActiveRef`). | ⏸️ Deferred (won't-do; 2 robustness fixes landed) |
| R13 | Android permission diet | Med | Med | Dropped 4 unused permissions from the release manifest. `RECORD_AUDIO` (no library declared it; app only plays audio with `allowsRecordingIOS: false`) and `SYSTEM_ALERT_WINDOW` (only RN's *debug* manifest re-adds it for the redbox; app uses `USE_FULL_SCREEN_INTENT`, not overlays) were plain deletes. `READ/WRITE_EXTERNAL_STORAGE` are merged in by `expo-file-system` (transitive, unused) and `expo-image` (unused) so they're stripped with `tools:node="remove"`. Also added `android.blockedPermissions` to `app.json` so a future `expo prebuild` won't reintroduce them. Verified against the merged release manifest (`processReleaseManifest`): all four are absent. | ✅ Done |
| R14 | Prune unused mobile deps | Med | Med | Removed 12 unused packages from `apps/mobile`: `react-native-webview`, `expo-blur`, `expo-image`, `expo-symbols`, `expo-web-browser`, `expo-system-ui`, `expo-haptics` (app uses RN `Vibration`, see R4); `@react-navigation/{native,bottom-tabs,elements}` (no direct imports — still provided transitively by `expo-router`, which declares them); and `react-native-web` + `react-dom` (only powered `expo start --web`, dropped the mobile `web` script too — the browser-rendered mobile target isn't used; web is the separate Next.js app). **Kept** `expo-constants` (declared **peer dependency of `expo-router`**) and the navigation runtime deps (`react-native-{safe-area-context,screens,reanimated,gesture-handler}`). Verified with mobile `typecheck` (clean) + `lint` (0 errors). **Native re-sync still needed before release:** run `expo prebuild` + `pod install` (and a gradle sync) so the committed `ios/android` projects + `Podfile.lock` drop the removed native modules, then a device build to confirm. | ✅ Done (wants native rebuild verification) |
| R15 | Replace `PLAY_STORE_LINK` placeholder | Med | Low | `LandingPage` ships `"https://google.com" // TODO`; confirm Android launch or hide the CTA. | ⏸️ Deferred (pending Play Store deployment) |
| R16 | Multi-level background notifications | Med | High | The repeating iOS notifications all show one fixed next-blind **by design**: since blinds advance only one level per expiry and then wait for player acknowledgement (see R12), there is exactly one "next blind" to announce until the app is reopened. The repeating notification is the deliberate workaround for iOS not allowing a background alarm — it nags "blinds went up" until acknowledged, then stops on open. A per-level timeline would only make sense if we wanted background auto-advance through multiple levels, which contradicts the acknowledgement model. Revisit only if that product decision changes. | ⏸️ Deferred (by design) |
| R17 | Gate `console.*` (mobile) | Low | Low | Added `src/utils/logger.ts` — a `console` wrapper that no-ops when `__DEV__` is false — and routed all ~75 mobile `console.*` calls (8 files) through it. Added an ESLint `no-console: error` rule (with `logger.ts` exempted) so direct `console` use can't creep back in. | ✅ Done |
| R18 | Remove dead `scheduledNotificationIds` plumbing | Low | Low | Dropped the never-populated `scheduledNotificationIds` state and the never-read `continuousDataRef` from `useTimerNotification`. `cancelNotification` (and the notification-tap handler) now call `clearAllNotifications`, which already does the real work via `cancelAllScheduledNotificationsAsync` + `dismissAllNotificationsAsync`. | ✅ Done |
| R19 | Consolidate `PokerSettings` `formatTime` | Low | Low | `PokerSettings` imports core `formatTime` instead of a local copy (cosmetic: pads minutes, `9:00`→`09:00`). | ✅ Done |
| R20 | Migrate `expo-av` → `expo-audio` | Med | Med | Replaced `expo-av` (`^16.0.8`) with `expo-audio` (`~0.4.8`, SDK 53's pinned version) and rewrote `useSounds` against the new API (`useAudioPlayer`/`useAudioPlayerStatus`, `setAudioModeAsync`; stop = `pause()` + `seekTo(0)` since expo-audio has no `stop()`). Audio-mode keys remapped (`allowsRecordingIOS`→`allowsRecording`, the numeric iOS/Android interruption modes→`'doNotMix'`, etc.). Typecheck/lint/test clean. **Native re-sync still needed before release:** `expo prebuild` + `pod install` (+ gradle sync) so the committed `ios/android` projects + `Podfile.lock` swap the native module, then a device build to confirm playback. | ✅ Done (wants native rebuild verification) |
| R21 | Hoist a shared timer state machine into `@poker/core` | High | High | Added `packages/core/src/timer/timerMachine.ts`: pure, framework-agnostic transitions (`startTimer`/`pauseTimer`/`resetTimer`/`tickTimer`/`advanceRound`/`withDuration`/`clampToDuration`/`hydrateTimerState`) over a shared `TimerMachineState`, with 22 new unit tests. Both `useWebTimer` and `useTimerEngine` are now thin adapters holding one `TimerMachineState` and delegating all state transitions to core; each keeps only its React effects, persistence wiring, callbacks, and genuine platform policy (web auto-advances on expiry / silently resets on load + full-resets on duration change; mobile stops-and-acknowledges / fires completion on reopen-to-expired + conservative duration change). Public hook APIs unchanged, so `PokerTimer`/`TimerContext` are untouched. Typecheck/lint/build/test clean. | ✅ Done (wants device smoke test) |
| R22 | Decompose `TimerContext` / `useTimerEngine` (SRP) | Med | High | Split engine vs. side-effects vs. native sync to reduce god-object fragility. `useTimerEngine` is now just the state-machine adapter (state, actions, tick interval, stop-and-acknowledge expiry); persistence moved to `useTimerPersistence` (AsyncStorage I/O + auto-save effect) and native sync to `useLiveActivitySync` (Live Activity / foreground-service mirroring). `TimerContext`'s expiry-alert + alarm-sound concern moved to `useTimerAlert` (`showAlert`/`clearAlert`, owns the alarm-playing ref and its unmount cleanup), leaving the context as a thin composition root (engine + blinds + notifications + permissions + app-state wiring). Public hook/context APIs unchanged, so `PokerTimer`/consumers are untouched. Typecheck/lint(0 errors)/test(50) clean. | ✅ Done (wants device smoke test) |
| R23 | Extract `<StoreButton>` in `LandingPage` | Low | Low | Apple/Google store SVG + button markup was duplicated 3× (mobile hero, desktop hero, CTA). Added a `StoreButton` component driven by a `STORE_CONFIG` map (href/label/gradient/SVG path per store), with `className`/`iconClassName` props for the per-site layout (full-width mobile, auto-width desktop, larger CTA icon). The CTA's redundant `<a><div>` wrapper was flattened into the shared single-anchor markup (the `group`/`overflow-hidden`/`relative` classes had no effect — no group-hover children). Typecheck/lint clean. | ✅ Done |
| R24 | Dependency / Expo SDK version bumps | Med | Med | Brought the stack current. **Web:** Next.js 15.3.5 → 16.2.7. **Mobile: Expo SDK 53 → 56** — RN 0.79.5 → 0.85.3, React 19.0 → 19.2.3, `react-native-reanimated` v3.17 → v4.3.1 (+ its new required peer `react-native-worklets` 0.8.3), `expo-router` 5 → 56.2.10, and every `expo-*`/`react-native-*` package realigned to the SDK 56 bundled versions (via `bundledNativeModules`). `@react-native-async-storage/async-storage` kept at 3.1.1 (latest, newer than the bundled 2.2.0) and `typescript` held at 5.8.3 (TS 6 bump deferred) — both documented in mobile `expo.install.exclude`. **React single-version invariant:** bumped web `react`/`react-dom` to 19.2.3 (pinned exact to match Expo), updated root `overrides` to `~19.2` for `@types/react*`, and added `react`/`react-dom: 19.2.3` overrides so the monorepo resolves one React copy (otherwise next's peer range floats a 2nd copy to root). Removed `newArchEnabled` from `app.json` (dropped from the SDK 56 config schema; New Arch is default — native `gradle.properties` flag retained). **Pre-existing issues surfaced by the clean reinstall & fixed:** (1) core's stale `@eslint/js@^10` (incompatible with eslint 9) → `^9.39.4`; (2) `next` failing to hoist to the workspace root on a fresh lockfile, which broke `eslint-config-next`'s `require("next/…")` (web lint) — pinned by declaring `next` as a root devDependency so it always hoists. **Verified at JS level:** typecheck/lint(0 errors)/test(50)/web build all clean; `expo-doctor` 20/21 (only the pre-existing bare-workflow non-CNG config warning remains); Metro/Hermes iOS bundle `expo export` compiles the full RN 0.85 + reanimated v4 + worklets graph cleanly. **iOS deployment target:** SDK 56's `expo-modules-core` requires iOS **16.4** (was 15.1), so added `expo-build-properties` (`~56.0.19`) with `ios.deploymentTarget: "16.4"` in `app.json` — without it `pod install` fails to link `ExpoModulesCore` ("requires iOS 16.4 but app targets 15.1"). **Native re-sync still needed before release:** run `expo prebuild` (regenerates the committed `ios/android` projects for RN 0.85 / reanimated v4 / worklets and writes the 16.4 target into `Podfile.properties.json`), then `cd ios && pod install` (+ Android gradle sync), then a device build to confirm runtime. **Gotcha hit during re-sync:** `expo prebuild` auto-infers and rewrites the app identifier if `ios.bundleIdentifier`/`android.package` aren't pinned in `app.json` — it silently renamed `com.toondeboer.pokerkit` → `com.toondeboer98.pokerkit` (and moved all native sources to the new package path). Both keys are now pinned to `com.toondeboer.pokerkit` in `app.json` to prevent identity drift. | ✅ Done (wants native rebuild verification) |
